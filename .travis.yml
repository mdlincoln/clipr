# Sample .travis.yml for R projects

language: r
r:
  - oldrel
  - release
  - devel
warnings_are_errors: true
cache: packages
sudo: false

addons:
  apt:
    packages:
      - xclip

after_success: R -e "devtools::revdep_check()"
