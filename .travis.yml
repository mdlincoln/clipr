# Sample .travis.yml for R projects

language: r
r:
  - oldrel
  - release
  - devel
warnings_are_errors: true
cache: packages
sudo: false
r_check_revdep: true

addons:
  apt:
    packages:
      - xclip

