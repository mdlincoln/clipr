# Sample .travis.yml for R projects

language: r
warnings_are_errors: true
sudo: true

env:
 global:
   - CRAN: http://cran.rstudio.com

apt-packages:
  - xclip

notifications:
  email:
    on_success: change
    on_failure: change
