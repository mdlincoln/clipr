# Sample .travis.yml for R projects

language: r
r:
  - oldrel
  - release
  - devel
env:
  - DISPLAY=:90.0
warnings_are_errors: true
cache: packages
sudo: false
r_check_revdep: true

addons:
  apt:
    packages:
      - xclip

before_script:
  - sh -e /etc/init.d/xvfb start
  - sleep 3
